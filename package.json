{
  "name": "continuum-intelligence-v3",
  "version": "3.0.0",
  "description": "Continuum Intelligence — ACH-based equity research platform for ASX stocks",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint scripts/ src/ public/js/ --ext .js",
    "lint:fix": "eslint scripts/ src/ public/js/ --ext .js --fix",
    "test": "jest --verbose",
    "test:ci": "jest --ci --coverage",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "test:all": "jest --verbose && vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "extract-data": "node scripts/extract-stock-data.js",
    "validate": "npm run lint && npm run test:all"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "dompurify": "^3.3.3",
    "marked": "^17.0.4",
    "yahoo-finance2": "^3.13.1"
  }
}
